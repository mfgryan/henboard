version: '3'
services:
    client:
        image: mfgryan/henclient:latest
        entrypoint: /bin/bash
        command: -c "npm install; npm run test -- --coverage" 
        volumes:
            - client_node_modules:/home/nginx/node_modules
        environment:
            ENV: prod
            CI: "true"
volumes:
    client_node_modules:
